// schema variables
const schema = {
    descriptionField : "plot",
    titleField : "title",
    imageField : "poster",
    vectorField : "mistral_plot_embedding",
    vectorSourceField: "plot"
}

export default schema;